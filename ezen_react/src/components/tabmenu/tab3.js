import React from 'react';

const Tab3 = (data) => {
    
    console.log(data)
    return (
        <>
            
            <h2>{JSON.stringify(data)}</h2>
        </>
    );
};

export default Tab3;